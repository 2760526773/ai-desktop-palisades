<Window x:Class="Palisades.View.Palisade"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Palisades.View"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:helpers="clr-namespace:Palisades.Helpers"
        xmlns:viewModel="clr-namespace:Palisades.ViewModel"
        xmlns:converters="clr-namespace:Palisades.Converters"
        helpers:WindowBlur.IsEnabled="False"
        helpers:WindowSinker.AlwaysOnBottom="True"
        helpers:WindowTaskViewHider.HideFromTaskView="True"
        ShowInTaskbar="False"
        mc:Ignorable="d"
        Title="桌面栅栏"
        Width="{Binding Width, Mode=TwoWay}"
        Height="{Binding Height, Mode=TwoWay}"
        MinWidth="260"
        MinHeight="32"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="Transparent"
        Left="{Binding FenceX, Mode=TwoWay}"
        Top="{Binding FenceY, Mode=TwoWay}"
        UseLayoutRounding="True">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="4" CornerRadius="2" GlassFrameThickness="0" NonClientFrameEdges="None" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.DataContext>
        <viewModel:PalisadeViewModel />
    </Window.DataContext>

    <Window.Resources>
        <converters:PathToImageConverter x:Key="PathToImage" />
        <converters:MultiValueEqualityConverter x:Key="MultiValueEqualityConverter" />
        <helpers:BindingProxy x:Key="proxy" Data="{Binding}" />

        <Style x:Key="HeaderButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="22" />
            <Setter Property="Height" Value="22" />
            <Setter Property="Foreground" Value="#F7FCFF" />
            <Setter Property="Background" Value="#14000000" />
            <Setter Property="BorderBrush" Value="#22FFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="2,0,2,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="11">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#28FFFFFF" />
                    <Setter Property="BorderBrush" Value="#55FFFFFF" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="KeyDown">
            <i:InvokeCommandAction Command="{Binding DelKeyPressed}" CommandParameter="{Binding}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Border x:Name="LayoutRoot" BorderThickness="1" BorderBrush="#74CFE5FF" SnapsToDevicePixels="True">
        <Border.Background>
            <SolidColorBrush Color="#304B82D6" />
        </Border.Background>

        <DockPanel LastChildFill="True">
            <Grid DockPanel.Dock="Top"
                  x:Name="Header"
                  Height="30"
                  MouseLeftButtonDown="Header_MouseLeftButtonDown"
                  IsHitTestVisible="True"
                  Background="#00000000"
                  Tag="{Binding DataContext, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}">
                <Grid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="&#x7F16;&#x8F91;&#x6805;&#x680F;" Command="{Binding Path=Data.EditPalisadeCommand, Source={StaticResource proxy}}" CommandParameter="{Binding}" />
                        <MenuItem Header="&#x5220;&#x9664;&#x6805;&#x680F;" Command="{Binding Path=Data.DeletePalisadeCommand, Source={StaticResource proxy}}" CommandParameter="{Binding Identifier}" />
                        <MenuItem Header="&#x53D6;&#x6D88;&#x5206;&#x7C7B;&#x5E76;&#x79FB;&#x56DE;&#x684C;&#x9762;" Click="UnarchiveFenceToDesktop_Click" />
                        <Separator />
                        <MenuItem Header="&#x65B0;&#x5EFA;&#x6805;&#x680F;" Command="{Binding Path=Data.NewPalisadeCommand, Source={StaticResource proxy}}" />
                        <Separator />
                        <MenuItem Header="&#x5173;&#x4E8E;" Command="{Binding Path=Data.OpenAboutCommand, Source={StaticResource proxy}}" CommandParameter="{Binding}" />
                        <MenuItem Header="AI &#x8BBE;&#x7F6E;" Click="OpenAiSettings_Click" />
                        <MenuItem Header="&#x7ACB;&#x5373;AI&#x5206;&#x7C7B;" Click="RunAiClassifyNow_Click" />
                        <MenuItem x:Name="AutoStartMenuItem" Header="&#x5F00;&#x673A;&#x81EA;&#x542F;&#x52A8;" IsCheckable="True" Click="ToggleAutoStart_Click" />
                        <Separator />
                        <MenuItem Header="&#x91CD;&#x542F;" Click="RestartApplication_Click" />
                        <MenuItem Header="&#x9000;&#x51FA;" Click="ExitApplication_Click" />
                    </ContextMenu>
                </Grid.ContextMenu>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="70" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4,0,0,0">
                    <Button x:Name="CollapseButton" Style="{StaticResource HeaderButtonStyle}" Content="^" ToolTip="展开/收起" Click="ToggleCollapse_Click" />
                </StackPanel>

                <TextBlock Grid.Column="1"
                           Text="{Binding Name}"
                           Foreground="#F7FCFF"
                           FontSize="14"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0">
                    <Button x:Name="LockButton" Style="{StaticResource HeaderButtonStyle}" Content="🔓" ToolTip="锁定/解锁" Click="ToggleLock_Click" />
                    <Button x:Name="MenuButton" Style="{StaticResource HeaderButtonStyle}" Content="☰" ToolTip="菜单" Click="OpenMenu_Click" />
                </StackPanel>
            </Grid>

            <Grid x:Name="drag" AllowDrop="True" Margin="0" Background="#00000000">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Drop">
                        <i:InvokeCommandAction Command="{Binding DropShortcut}" PassEventArgsToCommand="True" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="6,2,6,4">
                    <ItemsControl ItemsSource="{Binding Shortcuts}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel ItemWidth="72" ItemHeight="76" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ContentControl Tag="{Binding}"
                                                PreviewMouseLeftButtonDown="Shortcut_PreviewMouseLeftButtonDown"
                                                PreviewMouseMove="Shortcut_PreviewMouseMove"
                                                PreviewMouseRightButtonDown="Shortcut_PreviewMouseRightButtonDown"
                                                MouseRightButtonUp="Shortcut_MouseRightButtonUp">
                                    <ContentControl.ToolTip>
                                        <ToolTip Placement="Mouse" Background="#D8202020" Foreground="White" BorderBrush="#66FFFFFF" BorderThickness="1" Padding="8,6" HasDropShadow="False">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                                <TextBlock Text="{Binding UriOrFileAction}" TextWrapping="Wrap" MaxWidth="320" Margin="0,4,0,0" />
                                            </StackPanel>
                                        </ToolTip>
                                    </ContentControl.ToolTip>
                                    <ContentControl.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="打开" Click="OpenShortcut_Click" CommandParameter="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <MenuItem Header="打开所在位置" Click="OpenShortcutLocation_Click" CommandParameter="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <MenuItem Header="移回桌面" Click="MoveShortcutToDesktop_Click" CommandParameter="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <Separator />
                                            <MenuItem Header="删除" Click="DeleteShortcut_Click" CommandParameter="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <MenuItem Header="属性" Click="OpenShortcutProperties_Click" CommandParameter="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        </ContextMenu>
                                    </ContentControl.ContextMenu>

                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="MouseDoubleClick">
                                            <i:LaunchUriOrFileAction Path="{Binding UriOrFileAction}" />
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="MouseDown">
                                            <i:InvokeCommandAction Command="{Binding Path=Data.ClickShortcut, Source={StaticResource proxy}}" CommandParameter="{Binding}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>

                                    <Border Width="68" Height="72" Background="#00000000" Margin="2,1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#00000000" />
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#1AFFFFFF" />
                                                    </Trigger>
                                                    <DataTrigger Value="True">
                                                        <DataTrigger.Binding>
                                                            <MultiBinding Converter="{StaticResource MultiValueEqualityConverter}">
                                                                <Binding Mode="OneWay" />
                                                                <Binding Path="Data.SelectedShortcut" Source="{StaticResource proxy}" Mode="OneWay" />
                                                            </MultiBinding>
                                                        </DataTrigger.Binding>
                                                        <Setter Property="Background" Value="#24FFFFFF" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Cursor="Hand">
                                            <Image Source="{Binding IconPath, Converter={StaticResource PathToImage}}"
                                                   Width="32"
                                                   Height="32"
                                                   Stretch="UniformToFill"
                                                   Margin="0,2,0,3" />
                                            <TextBlock Text="{Binding Name}"
                                                       Foreground="#FFFFFF"
                                                       FontSize="10"
                                                       TextAlignment="Center"
                                                       TextWrapping="Wrap"
                                                       MaxHeight="30"
                                                       TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                    </Border>
                                </ContentControl>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </DockPanel>
    </Border>
</Window>

