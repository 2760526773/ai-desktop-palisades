<Window x:Class="Palisades.View.AiSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AI 设置"
        Width="640"
        Height="430"
        MinWidth="640"
        MinHeight="430"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent">
    <Window.Resources>
        <SolidColorBrush x:Key="AiPanelBrush" Color="#EEF7FB" />
        <SolidColorBrush x:Key="AiStrokeBrush" Color="#D4E4F2" />
        <SolidColorBrush x:Key="AiTitleBrush" Color="#16324A" />
        <SolidColorBrush x:Key="AiTextBrush" Color="#4A6785" />
        <SolidColorBrush x:Key="AiFieldBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="AiPrimaryBrush" Color="#4F8FF7" />
        <SolidColorBrush x:Key="AiPrimaryHoverBrush" Color="#3B7DE8" />

        <Style x:Key="TitleButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Foreground" Value="{StaticResource AiTitleBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5EFF8" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AiTextBrush}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style x:Key="GlassTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="{StaticResource AiTitleBrush}" />
            <Setter Property="Background" Value="{StaticResource AiFieldBrush}" />
            <Setter Property="BorderBrush" Value="#D4E4F2" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="14">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AiPrimaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GlassComboBoxStyle" TargetType="ComboBox">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="{StaticResource AiTitleBrush}" />
            <Setter Property="Background" Value="{StaticResource AiFieldBrush}" />
            <Setter Property="BorderBrush" Value="#D4E4F2" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="IsEditable" Value="False" />
        </Style>

        <Style x:Key="EditableModelComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource GlassComboBoxStyle}">
            <Setter Property="IsEditable" Value="True" />
            <Setter Property="IsTextSearchEnabled" Value="False" />
            <Setter Property="StaysOpenOnEdit" Value="True" />
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource AiTitleBrush}" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="#D4E4F2" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource AiPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="#00000000" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AiPrimaryHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Margin="18" Background="{StaticResource AiPanelBrush}" BorderBrush="{StaticResource AiStrokeBrush}" BorderThickness="1" CornerRadius="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="56" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="18,10,12,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="AI 设置" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource AiTitleBrush}" />
                    <TextBlock Text="模型提供商与 API 配置" Margin="12,3,0,0" FontSize="12" Foreground="{StaticResource AiTextBrush}" />
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource TitleButtonStyle}" Content="✕" Click="Cancel_Click" />
            </Grid>

            <Grid Grid.Row="1" Margin="22,6,22,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,16">
                    <TextBlock Text="提供商" Style="{StaticResource FieldLabelStyle}" />
                    <ComboBox x:Name="ProviderCombo" Style="{StaticResource GlassComboBoxStyle}" MinHeight="46" SelectionChanged="ProviderCombo_SelectionChanged" />
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="10,0,0,16">
                    <TextBlock Text="模型名" Style="{StaticResource FieldLabelStyle}" />
                    <ComboBox x:Name="ModelCombo" Style="{StaticResource EditableModelComboBoxStyle}" MinHeight="46" IsTextSearchEnabled="False" />
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,0,0,16">
                    <TextBlock Text="接口地址" Style="{StaticResource FieldLabelStyle}" />
                    <TextBox x:Name="BaseUrlText" Style="{StaticResource GlassTextBoxStyle}" MinHeight="46" />
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="环境变量名" Style="{StaticResource FieldLabelStyle}" />
                    <TextBox x:Name="EnvKeyText" Style="{StaticResource GlassTextBoxStyle}" MinHeight="46" />
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="API Key" Style="{StaticResource FieldLabelStyle}" />
                    <PasswordBox x:Name="ApiKeyPassword" FontSize="16" Padding="14,10" MinHeight="46" Background="{StaticResource AiFieldBrush}" BorderBrush="#D4E4F2" BorderThickness="1" PasswordChar="●" />
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,22,22">
                <Button Width="104" Margin="0,0,10,0" Content="取消" Style="{StaticResource SecondaryButtonStyle}" Click="Cancel_Click" />
                <Button Width="132" Content="保存并生效" Style="{StaticResource PrimaryButtonStyle}" Click="Save_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
